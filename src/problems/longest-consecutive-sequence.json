{
  "id": "longest-consecutive-sequence",
  "title": "Longest Consecutive Sequence",
  "difficulty": "Medium",
  "topic": "Arrays & Hashing",
  "prompt": "Given an unsorted array of integers nums, return the length of the longest consecutive elements sequence. You must write an algorithm that runs in O(n) time.",
  "examples": [
    {"input": "nums = [100,4,200,1,3,2]", "output": "4"},
    {"input": "nums = [0,3,7,2,5,8,4,6,0,1]", "output": "9"}
  ],
  "constraints": [
    "0 <= nums.length <= 10^5",
    "-10^9 <= nums[i] <= 10^9"
  ],
  "jsStarter": "/**\n * Longest Consecutive Sequence\n * @param {number[]} nums\n * @return {number}\n */\nfunction longestConsecutive(nums) {\n  // Talk through your approach out loud like an interview.\n}",
  "cppStarter": "#include <vector>\n#include <unordered_set>\nusing namespace std;\n\nclass Solution {\npublic:\n    int longestConsecutive(vector<int>& nums) {\n        // Talk through your approach out loud like an interview.\n    }\n};",
  "tests": [],
  "hints": [
    "Put all numbers in a set for O(1) lookup. Only start counting from the beginning of a sequence.",
    "A number is the start of a sequence if num-1 is not in the set. Then count while num+1 is in the set.",
    "This ensures each sequence is only counted once, giving O(n) total time.",
    "Example: For 1,2,3,4 - only 1 starts a sequence. 2,3,4 are skipped as starters."
  ],
  "solution": {
    "javascript": "function longestConsecutive(nums) {\n  const set = new Set(nums);\n  let maxLen = 0;\n  for (const n of set) {\n    if (set.has(n - 1)) continue;\n    let len = 0, cur = n;\n    while (set.has(cur)) { len++; cur++; }\n    maxLen = Math.max(maxLen, len);\n  }\n  return maxLen;\n}",
    "cpp": "int longestConsecutive(vector<int>& nums) {\n    unordered_set<int> set(nums.begin(), nums.end());\n    int maxLen = 0;\n    for (int n : set) {\n        if (set.count(n - 1)) continue;\n        int len = 0, cur = n;\n        while (set.count(cur)) { len++; cur++; }\n        maxLen = max(maxLen, len);\n    }\n    return maxLen;\n}"
  }
}
