{
  "id": "validate-binary-search-tree",
  "title": "Validate Binary Search Tree",
  "difficulty": "Medium",
  "topic": "Trees",
  "prompt": "Given the root of a binary tree, determine if it is a valid binary search tree (BST). A valid BST is defined as follows: The left subtree of a node contains only nodes with keys less than the node's key. The right subtree of a node contains only nodes with keys greater than the node's key. Both the left and right subtrees must also be binary search trees.",
  "examples": [
    {"input": "root = [2,1,3]", "output": "true"},
    {"input": "root = [5,1,4,null,null,3,6]", "output": "false"}
  ],
  "constraints": [
    "1 <= number of nodes <= 10^4",
    "-2^31 <= Node.val <= 2^31 - 1"
  ],
  "jsStarter": "/**\n * Validate Binary Search Tree\n * @param {TreeNode} root\n * @return {boolean}\n */\nfunction isValidBST(root) {\n  // Talk through your approach out loud like an interview.\n}",
  "cppStarter": "class Solution {\npublic:\n    bool isValidBST(TreeNode* root) {\n        // Talk through your approach out loud like an interview.\n    }\n};",
  "tests": [],
  "hints": [
    "Pass (min, max) bounds down. For left subtree, max becomes current value. For right subtree, min becomes current value.",
    "At each node: value must be > min and < max. Recursively validate left with (min, node.val) and right with (node.val, max).",
    "Use null/infinity for unbounded. In JS: -Infinity and Infinity work.",
    "O(n) time. In-order traversal (check ascending) is another approach."
  ],
  "solution": {
    "javascript": "function isValidBST(root) {\n  function valid(node, min, max) {\n    if (!node) return true;\n    if (node.val <= min || node.val >= max) return false;\n    return valid(node.left, min, node.val) && valid(node.right, node.val, max);\n  }\n  return valid(root, -Infinity, Infinity);\n}",
    "cpp": "bool isValidBST(TreeNode* root) {\n    function<bool(TreeNode*, long, long)> valid = [&](TreeNode* node, long minVal, long maxVal) {\n        if (!node) return true;\n        if (node->val <= minVal || node->val >= maxVal) return false;\n        return valid(node->left, minVal, node->val) && valid(node->right, node->val, maxVal);\n    };\n    return valid(root, LONG_MIN, LONG_MAX);\n}"
  }
}
